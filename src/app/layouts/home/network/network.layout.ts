import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-network',
  imports: [RouterModule],
  templateUrl: './network.layout.html',
  styleUrl: './network.layout.scss',
})
export class NetworkLayout {}
